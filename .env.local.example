# Sanity CMS Configuration
# Get these values from https://www.sanity.io/manage

# Required: Your Sanity project ID
# Your project ID: ktdunyve
NEXT_PUBLIC_SANITY_PROJECT_ID=ktdunyve

# Required: Dataset name (usually 'production')
# Find this in: Project Settings → Datasets
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: API version (use current date or 2024-01-01)
# Format: YYYY-MM-DD
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Required for draft mode: Read API token
# Generate this in: Project Settings → API → Add API token
# Permissions: Read (Viewer)
# This token should be kept secret and never committed to version control
SANITY_API_TOKEN=your_api_token_here

# Optional: Revalidate secret for webhook on-demand revalidation (future feature)
# Generate a random string (e.g., using: openssl rand -base64 32)
SANITY_REVALIDATE_SECRET=your_random_secret_here

# Studio Configuration (required for Docker build)
# Should match the project ID and dataset above
SANITY_STUDIO_PROJECT_ID=ktdunyve
SANITY_STUDIO_DATASET=production
